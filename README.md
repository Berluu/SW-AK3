# SW-AK3
This script was made for task 4 from AK3 in Serverdrift med Windows, it saves the DHCP leases from a DHCP server to a .csv file.

The server name and location the csv file gets saved to should be changed to match the environment they're in.
